using CampaingModel;
using DataAccess.Data;

namespace MinimalAPI
{
    public static class apiTwitter
    {
        /*
        public static void ConfigureApi(this WebApplication app)
        {
            // All of my API endpoint mapping
           
            app.MapGet("/Campaing/{id}", GetTwitter);
            app.MapPost("/CampaignSupport", InsertTitter);
            app.MapPut("/Campaing", UpdateTwitter);
            app.MapDelete("/Campaing", DeleteTwitter);
            app.MapGet("/Campaing", GetTwitters);
        }
        */
    }
}

